{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "exampen/events/page.ready",
  "title": "page.ready event",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "event_id",
    "event_type",
    "event_version",
    "occurred_at",
    "exam_id",
    "student_id",
    "artifact_id",
    "page_number",
    "exam_type",
    "authoritative_source"
  ],
  "properties": {
    "event_id": { "type": "string" },
    "event_type": { "const": "page.ready" },
    "event_version": { "const": "2.0.0" },
    "occurred_at": { "type": "string", "format": "date-time" },
    "exam_id": { "type": "string" },
    "student_id": { "type": "string" },
    "artifact_id": { "type": "string" },
    "page_number": { "type": "integer" },
    "exam_type": { "type": "string", "enum": ["dcr", "pcr"] },
    "authoritative_source": {
      "type": "string",
      "enum": ["strokes", "copy_image", "both"]
    },
    "vector_uri": { "type": "string" },
    "image_uri": { "type": "string" },
    "routed_engine": { "type": "string", "enum": ["dcr", "pcr"] }
  }
}

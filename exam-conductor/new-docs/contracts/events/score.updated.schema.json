{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "exampen/events/score.updated",
  "title": "score.updated event",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "event_id",
    "event_type",
    "event_version",
    "occurred_at",
    "exam_id",
    "student_id",
    "lifecycle_state",
    "total_score",
    "reason"
  ],
  "properties": {
    "event_id": { "type": "string" },
    "event_type": { "const": "score.updated" },
    "event_version": { "const": "2.0.0" },
    "occurred_at": { "type": "string", "format": "date-time" },
    "exam_id": { "type": "string" },
    "student_id": { "type": "string" },
    "exam_type": { "type": "string", "enum": ["dcr", "pcr"] },
    "lifecycle_state": {
      "type": "string",
      "enum": ["draft", "reviewed", "finalized", "published", "locked"]
    },
    "total_score": { "type": "number" },
    "reason": {
      "type": "string",
      "enum": ["engine_result_created", "override_applied", "published", "rescored"]
    }
  }
}

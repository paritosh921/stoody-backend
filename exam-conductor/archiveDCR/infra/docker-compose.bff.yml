# =============================================================
# ExamPen — BFF service placeholders (commented out)
# Usage: docker compose -f docker-compose.yml \
#        -f docker-compose.services.yml \
#        -f docker-compose.bff.yml up
# =============================================================
version: "3.8"

x-service-env: &service-env
  DATABASE_HOST: postgres
  DATABASE_PORT: "5432"
  DATABASE_USER: ${POSTGRES_USER:-exampen}
  DATABASE_PASSWORD: ${POSTGRES_PASSWORD:-exampen_dev}
  NATS_URL: nats://nats:4222
  MINIO_URL: http://minio:9000
  MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-exampen}
  MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-exampen_minio_dev}
  REDIS_URL: redis://:${REDIS_PASSWORD:-exampen_redis_dev}@redis:6379/0
  AUTH_SERVICE_URL: http://svc-auth:8000

services:
  # -- svc-teacher-bff (port 8013) --
  # svc-teacher-bff:
  #   build: ../services/svc-teacher-bff
  #   environment:
  #     <<: *service-env
  #     SCORE_ENGINE_URL: http://svc-score-engine:8006
  #     ANALYTICS_URL: http://svc-analytics:8008
  #     REVIEW_URL: http://svc-review:8007
  #     PLAGIARISM_URL: http://svc-plagiarism:8009
  #     CHAT_URL: http://svc-chat:8010
  #   ports: ["8013:8013"]
  #   depends_on: [redis]
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:8013/health"]
  #     interval: 10s
  #     timeout: 3s
  #     retries: 5
  #   networks: [exampen-net]

  # -- svc-student-bff (port 8014) --
  # svc-student-bff:
  #   build: ../services/svc-student-bff
  #   environment:
  #     <<: *service-env
  #     SCORE_ENGINE_URL: http://svc-score-engine:8006
  #     ANALYTICS_URL: http://svc-analytics:8008
  #     REVIEW_URL: http://svc-review:8007
  #     CHAT_URL: http://svc-chat:8010
  #   ports: ["8014:8014"]
  #   depends_on: [redis]
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:8014/health"]
  #     interval: 10s
  #     timeout: 3s
  #     retries: 5
  #   networks: [exampen-net]

  # -- svc-invig-console (port 8015) --
  # svc-invig-console:
  #   build: ../services/svc-invig-console
  #   environment:
  #     <<: *service-env
  #     EXAM_ORCH_URL: http://svc-exam-orch:8001
  #   ports: ["8015:8015"]
  #   depends_on: [nats, redis]
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:8015/health"]
  #     interval: 10s
  #     timeout: 3s
  #     retries: 5
  #   networks: [exampen-net]

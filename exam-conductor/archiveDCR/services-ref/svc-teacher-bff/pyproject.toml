[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "svc-teacher-bff"
version = "0.1.0"
description = "ExamPen teacher BFF — read-only aggregation layer for teacher UI"
requires-python = ">=3.12"
license = { text = "Proprietary" }

dependencies = [
    "exampen-common",
    "aiohttp>=3.9,<4",
    "fastapi>=0.111,<1",
    "pyjwt[crypto]>=2.8,<3",
    "uvicorn[standard]>=0.29,<1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8,<9",
    "pytest-asyncio>=0.23,<1",
    "httpx>=0.27,<1",
    "aioresponses>=0.7,<1",
    "coverage>=7,<8",
]

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
